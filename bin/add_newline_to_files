#!/bin/bash

HTEMP=/tmp/newline_temp
for filename in $(find . -name "$1" -type f); do
	COUNT=`wc -l $filename | cut -d' ' -f1`
	head -$COUNT $filename >"$HTEMP"
	if ! cmp $filename "$HTEMP" &>/dev/null; then
		echo >>$filename
	fi
	rm -f "$HTEMP"
done
